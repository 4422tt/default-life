# Public URL of your own server-side order recognition endpoint.
# The endpoint receives multipart/form-data with one or more `images` fields.
NEXT_PUBLIC_ORDER_RECOGNITION_ENDPOINT=https://your-backend.example.com/api/import-orders

# Keep provider secrets on that backend only. Never expose them with NEXT_PUBLIC_.
# OPENAI_API_KEY=configure-this-on-the-backend
